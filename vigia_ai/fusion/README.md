# Fusion Engine

Baseline research determinista con `ObservationEvidence`, familias, clustering geodésico,
persistencia, roles y `as_of`. No produce probabilidad. Especificación: `docs/fusion-engine.md`.
